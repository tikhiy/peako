'use strict';

// var keys = require( '../keys' );

module.exports = function baseIsMatch ( /*src, obj*/ ) {
  // var k, i, a, b;

  // for ( k = keys( src ), i = k.length - 1; i >= 0; --i ) {
  //   a = src[ k[ i ] ];
  //   b = obj[ k[ i ] ];

  //   if ( a === b ) {
  //     continue;
  //   }

  //   if ( _type( a ) === 'array' ) {

  //   }

  //   if ( isObjectLike( a ) ) {
  //     if ( ! isObjectLike( b ) || ! baseIsMatch( a, b ) ) {
  //       return false;
  //     }

  //     continue;
  //   }
  // }

  // return true;
};
